--- ./motd	2020-11-19 18:44:39.201571000 -0500
+++ /etc/rc.d/motd	2020-09-21 02:16:13.326931000 -0400
@@ -1,6 +1,6 @@
 #!/bin/sh
 #
-# $FreeBSD: releng/12.2/libexec/rc/rc.d/motd 298514 2016-04-23 16:10:54Z lme $
+# $FreeBSD$
 #
 
 # PROVIDE: motd
@@ -35,7 +35,7 @@
 
 	T=`mktemp -t motd`
 	uname -v | sed -e 's,^\([^#]*\) #\(.* [1-2][0-9][0-9][0-9]\).*/\([^\]*\) $,\1 (\3) #\2,' > ${T}
-	awk '{if (NR == 1) {if ($1 == "FreeBSD") {next} else {print "\n"$0}} else {print}}' < /etc/motd >> ${T}
+	awk '{if (NR == 1) {if ($1 == "FreeBSD" || $1 == "AS-BSD") {next} else {print "\n"$0}} else {print}}' < /etc/motd >> ${T}
 
 	cmp -s $T /etc/motd || {
 		cp $T /etc/motd
